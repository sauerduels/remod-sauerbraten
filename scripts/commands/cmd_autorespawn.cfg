cmd_auto_respawn = [
    if (!=s $arg2 "") [
	    respawn_millis = $arg2
    ]
    say (concatword "^f1Auto respawn ^f0" $respawn_millis "^f1 milliseconds after death")
]

registercommand "autorespawn" cmd_auto_respawn 3 "|i" "autorespawn [milliseconds] ^f1Time to force respawn at after death"
